<% if user_signed_in? %>
<div class="container">
  <h1>Nuevo Post</h1>
  <% if @post.errors.any? %>
    <% @post.errors.full_messages.each do |message| %>
      <li class ="error"><%= message %></li>
    <% end %>
  <% end %>
  <%= form_for @post do |f| %>
    <div>
      <b><%= f.label "Título"  %></b><br>
      <%= f.text_field :title, class: "titulo"   %>
    </div>
    <div>
      <b><%= f.label "Cuerpo" %></b><br>
      <%= f.text_area :content, class: "cuerpo"  %>
    </div>
    <%= f.hidden_field :author, :value => current_user.email %>
    <div>
      <%= f.submit  'Crear post', class: "btn btn-primary"  %>
    </div>
  <% end %>

</div>
<%else%>
  <h1>Error: Debe registrarse como usuario</h1>
<%end%>
