<!DOCTYPE html>
<html>
  <head>
    <title>Super Blog | Rails</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
      <a class="navbar-brand" href="/posts"><b>Super blog</b></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to " Nuevo post ", new_post_path, class: "link text-white" %>
            </li>
            <li class="nav-item">
              <%= link_to " Cerrar sesión ", destroy_user_session_path, :method => :delete , data: {confirm: "¿Estás seguro?"}, class: "link text-white"  %>
            </li>
          <%else%>
            <li class="nav-item active">
              <%= link_to " Ingresar  ", new_user_session_path, class: "link text-white"  %>
            </li>
            <li class="nav-item text-white">
              <%= link_to " Registrarse  ", new_user_registration_path, class: "link text-white"  %>
            </li>
          <%end%>
        </ul>
      </div>
      </div>
    </nav>

    <div class="container">
      <% if  not notice == nil%>
      <p class="alert alert-success alert-dismissible fade show"><%= notice %></p>
      <%end%>
      <% if not alert == nil%>
      <p class="alert alert-danger alert-dismissible fade show"><%= alert %></p>
      <%end%>
    </div>

    <%= yield %>
  </body>
</html>
